# Rate limits transcribed from the provider's own documentation — see provenance. Quoted, not derived.
generated: '2026-09-20'
method: searched
source: https://docs.jfrog.com/artifactory/docs/aql-performance
sources:
- https://docs.jfrog.com/artifactory/docs/aql-performance
provenance:
  tool: planning/api-economics/harvest_ratelimits.py
  run: 20260920T133210
  grounding: every limit_text was found verbatim in the fetched page text
published: true
note: Page documents AQL concurrency rate limiting (default 3 parallel calls, 429 after 10000ms pending
  timeout), result set limits (500,000), query string limit (6,000 chars, HTTP 400), and query timeouts
  (HTTP 408). No rate-limit headers documented.
limit_count: 4
limits:
- name: AQL concurrent query limit
  limit_text: Maximum number of AQL/search API calls that can run in parallel.
  scope: account
  plan: self-managed
  endpoint: search/AQL API
- name: Max result set size (SaaS)
  limit_text: a fixed value of 500,000 is set as the default maximal result set size to limit the search/AQL
    API call response
  limit: 500000
  scope: endpoint
  plan: Artifactory SaaS
  endpoint: search/AQL API
- name: aql.search.query.max.limit
  limit_text: aql.search.query.max.limit is set to 500,000 results
  limit: 500000
  scope: endpoint
  plan: Artifactory SaaS version 7.107.0 and above
  endpoint: search/AQL API
- name: Max AQL query string size
  limit_text: The default maximum is 6,000 characters.
  limit: 6000
  scope: endpoint
  endpoint: POST /artifactory/api/search/aql
exceeded_status: 429
